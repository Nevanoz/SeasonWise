{
  "name": "@musimaman/financial-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Pure deterministic financial calculation engine for MusimAman",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@musimaman/shared-types": "workspace:*",
    "@musimaman/config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
